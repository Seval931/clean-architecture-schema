
//minimum required functions/variables to create an acceptable adapter for the app
interface adapter1 {
    something: string;
    num: number;
    processThis(inputValues: number): number;
}

export { adapter1 };